---
title: "Mdxで書けるMarkdownのスタイル確認"
date: "2020-12-29-06-56"
tags: [mdx, markdown, blog]
---

これはテスト記事です
- これはテストのテキストです。
- これはThis is a pen;
- React compornent;
- Next.js

|  TH  |  TH  |  TH  |  TH  |
| ---- | ---- | ---- | ---- |
|  discord.jsにおいて、テーブルを  |  discord.jsにおいて、テーブルを  |  discord.jsにおいて、テーブルを  |  discord.jsにおいて、テーブルを  |
|  TD  |  TD  |  TD  |  TD  |

Overwatchのプレイヤーデータ自体はここから見れるのでスクレイピングして持ってくるのも一つですが、その辺りを代わりにやってくれるoverwatch-api(非公式)というものが提供されてるので利用させてもらいましょう。  
今回使用するパッケージをインストールします。

## これはテストです React Components - Next.js

discord.jsにおいて、テーブルをinlineに表示したい場合は`inline: true`を持たせることでfieldを横に最大3つまで並べることが可能です

> Botのいるチャンネルにoverwatch `<battletag> <region> <platform>` という形での投稿がされた時に指定されたプレイヤーの名前、レベル、レートをBotに返信させるサンプルコードを書いてみます。

Overwatchのプレイヤーデータ自体はここから見れるのでスクレイピングして持ってくるのも一つですが、その辺りを代わりにやってくれるoverwatch-api(非公式)というものが提供されてるので利用させてもらいましょう。  
今回使用するパッケージをインストールします。

## Code block

consoleに文字列を出力させる

```js
console.log('test desu')
const test = getPostData()
```

- - -

Markdownにおける引用のスタイルのテストをしてみます。
> Botのいるチャンネルにoverwatch `<battletag> <region> <platform>` という形での投稿がされた時に指定されたプレイヤーの名前、レベル、レートをBotに返信させるサンプルコードを書いてみます。

# H1テスト

discord.jsにおいて、テーブルをinlineに表示したい場合は`inline: true`を持たせることでfieldを横に最大3つまで並べることが可能です

> Botのいるチャンネルにoverwatch `<battletag> <region> <platform>` という形での投稿がされた時に指定されたプレイヤーの名前、レベル、レートをBotに返信させるサンプルコードを書いてみます。

Overwatchのプレイヤーデータ自体はここから見れるのでスクレイピングして持ってくるのも一つですが、その辺りを代わりにやってくれるoverwatch-api(非公式)というものが提供されてるので利用させてもらいましょう。  
今回使用するパッケージをインストールします。

## H2テスト

discord.jsにおいて、テーブルをinlineに表示したい場合は`inline: true`を持たせることでfieldを横に最大3つまで並べることが可能です

> Botのいるチャンネルにoverwatch `<battletag> <region> <platform>` という形での投稿がされた時に指定されたプレイヤーの名前、レベル、レートをBotに返信させるサンプルコードを書いてみます。

Overwatchのプレイヤーデータ自体はここから見れるのでスクレイピングして持ってくるのも一つですが、その辺りを代わりにやってくれるoverwatch-api(非公式)というものが提供されてるので利用させてもらいましょう。  
今回使用するパッケージをインストールします。

### H3テスト

discord.jsにおいて、テーブルをinlineに表示したい場合は`inline: true`を持たせることでfieldを横に最大3つまで並べることが可能です

> Botのいるチャンネルにoverwatch `<battletag> <region> <platform>` という形での投稿がされた時に指定されたプレイヤーの名前、レベル、レートをBotに返信させるサンプルコードを書いてみます。

Overwatchのプレイヤーデータ自体はここから見れるのでスクレイピングして持ってくるのも一つですが、その辺りを代わりにやってくれるoverwatch-api(非公式)というものが提供されてるので利用させてもらいましょう。  
今回使用するパッケージをインストールします。

#### H4テスト

```js
console.log('test desu')
const test = getPostData()
```

discord.jsにおいて、テーブルをinlineに表示したい場合は`inline: true`を持たせることでfieldを横に最大3つまで並べることが可能です

> Botのいるチャンネルにoverwatch `<battletag> <region> <platform>` という形での投稿がされた時に指定されたプレイヤーの名前、レベル、レートをBotに返信させるサンプルコードを書いてみます。

Overwatchのプレイヤーデータ自体はここから見れるのでスクレイピングして持ってくるのも一つですが、その辺りを代わりにやってくれるoverwatch-api(非公式)というものが提供されてるので利用させてもらいましょう。  
今回使用するパッケージをインストールします。
```js
console.log('test desu')
const test = getPostData()
```

##### H5テスト

discord.jsにおいて、テーブルをinlineに表示したい場合は`inline: true`を持たせることでfieldを横に最大3つまで並べることが可能です

> Botのいるチャンネルにoverwatch `<battletag> <region> <platform>` という形での投稿がされた時に指定されたプレイヤーの名前、レベル、レートをBotに返信させるサンプルコードを書いてみます。

Overwatchのプレイヤーデータ自体はここから見れるのでスクレイピングして持ってくるのも一つですが、その辺りを代わりにやってくれるoverwatch-api(非公式)というものが提供されてるので利用させてもらいましょう。  
今回使用するパッケージをインストールします。

```ts
import { parse, format } from 'date-fns'

export function formatDate(date: string) {
  const fmt = parse(date, 'yyyy-MM-dd-HH-mm', new Date())
  // return format(fmt, 'yyyy/MM/dd HH:mm')
  return format(fmt, 'yyyy/MM/dd')
}
```